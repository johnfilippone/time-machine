add usage to parser machine
find a way to use SMS to call parser machine
only allow integers for the first arg of parser machine
move web page into repo and put sym link in apache
generalize grey time to work for the timespan of the given records
add unit tests
